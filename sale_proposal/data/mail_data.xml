<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="email_template_sale_proposal" model="mail.template">
            <field name="name">Proposal Order: Send by email</field>
            <field name="model_id" ref="sale_proposal.model_proposal_order"/>
            <field name="subject">${object.company_id.name} (Ref ${object.name or 'n/a' })</field>
            <field name="email_from">${(object.user_id.email_formatted or user.email_formatted) | safe}</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px;">
    <p style="margin: 0px; padding: 0px; font-size: 13px;">
        % set doc_name = 'Proposal'
        Hello,
        <br/><br/>
        Your
        ${doc_name} <strong>${object.name}</strong>
        amounting in <strong>${format_amount(object.amount_total_proposed, object.pricelist_id.currency_id)}</strong> is ready for review.
        <br/><br/>
        Do not hesitate to contact us if you have any questions.
        <br/>
    </p>
</div>
            </field>
            <field name="report_template" ref="action_report_proposal_order"/>
            <field name="report_name">${(object.name or '').replace('/','_')}</field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="auto_delete" eval="True"/>
        </record>
    </data>
</odoo>