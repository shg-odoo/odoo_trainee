
<odoo>

    <template id="students_id" name="School Students">
        <t t-call="website.layout">
            <div class="container">
                Hello
                <table id="table" class="table table-bordered" border="1">
                    <thead>
                        <tr>
                            <td><h4> Name </h4></td>
                            <td><h4> Age </h4></td>
                            <td><h4> Gender </h4></td>
                            <td><h4> Company </h4></td>
                            <td><h4> Delete </h4></td>
                            <td><h4> Update </h4></td>
                        </tr>
                    </thead>
                    <form action="/add_student" class="inline">
                        <button class="float-left submit-button" >Add</button>
                        </form>
                    <tbody>
                        <t t-foreach="students" t-as="student">
                            <tr>
                                <td><t t-esc="student.name"/></td>
                                <td> <t t-esc="student.age"/> </td>
                                <td> <t t-esc="student.gender"/></td>
                                <td> <t t-esc="student.company_id"/></td>
                                <td><button onclick="removeSelectedRow(); alert('You are going to delete the row!!!')">Remove</button></td>
                                <td><button onclick="updateSelectedRow();">Edit</button></td>
                            </tr>
                </t>
            </tbody>
        </table>
        <script>

            var rIndes = document.getElementById("table");

            function removeSelectedRow()
            {
                table.deletedRow(rIndex);
            }
        </script>
        </div>
        </t>
    </template>

</odoo>