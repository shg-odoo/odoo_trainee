<?xml version="1.0" encoding="utf-8" ?>
<odoo>
	<!-- creating the category  -->
	<record id="category_administration" model="ir.module.category">
		<field name="name">Administration</field>
		<field name="description">Category for Student</field>
		<field name="sequence">1</field>
	</record>

	<!-- creating groups -->
	<record id="group_users" model="res.groups">
		<field name="name">User</field>
		<field name="category_id" ref="category_administration"/>
	</record>

	<record id="group_admin" model="res.groups">
		<field name="name">Admin</field>
		<field name="category_id" ref="category_administration"/>
		<field name="implied_ids" eval="[(4,ref('group_users'))]"/>
	</record>

	<!-- creating record rules -->
	<record id="his_own_task" model="ir.rule">
		<field name="name">Only his task</field>
		<field name="model_id" ref="model_other_task"/>
		<field name="domain_force">[("user_id", "=", user.id)]</field>
		<field name="groups" eval="[(4,ref('group_users'))]"/>
		<field name="perm_create" eval="1"/>
		<field name="perm_write" eval="1"/>
		<field name="perm_unlink" eval="1"/>
		<field name="perm_read" eval="1"/> 
	</record>

	<record id="see_all_task" model="ir.rule">
		<field name="name">all tasks</field>
		<field name="model_id" ref="model_other_task"/>
		<field name="domain_force">[(1, "=", 1)]</field>
		<field name="groups" eval="[(4,ref('group_admin'))]"/>
		<field name="perm_create" eval="1"/>
		<field name="perm_write" eval="1"/>
		<field name="perm_unlink" eval="1"/>
		<field name="perm_read" eval="1"/> 
	</record>

</odoo>